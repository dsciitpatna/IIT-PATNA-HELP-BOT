<html>
<head>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
  <title><%= title %></title>
  <style type="text/css">
    body {
      background-color: black;
      color: white;
    }
    #list{
      margin: auto;
    }
    #back{
      background-color: rgba(100,100,100,0.8);
      color: blue;
      border-radius: 50%;
    }

    a.but{
      display: table;
      width: 100px;
      height: 100px;
      top: 45%;
      left: 1%;
    }

    #ch{
      display: table-cell;
      vertical-align: middle;
    }
    #list th {
      text-align: left;
      background: darkslategrey;
      height: 50px;
      padding: 10px;
    }
    #list tr,td {
      padding: 10px;
    }
    #list tr:nth-child(odd){background-color: dimgrey;}
    #list tr:nth-child(even){background-color: slategray;}
    
    #logo {
      width: 100px;
      position: absolute;
      top: 20px;
      right: 20px;
    }
  </style>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
  <style type="text/css">
    @charset "UTF-8";
    @import url(https://fonts.googleapis.com/css?family=Open+Sans:300,400,700);

    body {
      font-family: 'Open Sans', sans-serif;
      font-weight: 300;
      line-height: 1.42em;
      color:#A7A1AE;
      background-color:#1F2739;
    }

    h1 {
      font-size:3em; 
      font-weight: 300;
      line-height:1em;
      text-align: center;
      color: #4DC3FA;
    }

    h2 {
      font-size:1em; 
      font-weight: 300;
      text-align: center;
      display: block;
      line-height:1em;
      padding-bottom: 2em;
      color: #FB667A;
    }

    h2 a {
      font-weight: 700;
      text-transform: uppercase;
      color: #FB667A;
      text-decoration: none;
    }

    .blue { color: #185875; }
    .yellow { color: #FFF842; }

    .container th h1 {
      font-weight: bold;
      font-size: 1em;
      text-align: left;
      color: #185875;
    }

    .container td {
      font-weight: normal;
      font-size: 1em;
      -webkit-box-shadow: 0 2px 2px -2px #0E1119;
      -moz-box-shadow: 0 2px 2px -2px #0E1119;
      box-shadow: 0 2px 2px -2px #0E1119;
    }
    #back{
      background-color: rgba(100,100,100,0.8);
      color: blue;
      border-radius: 50%;
    }

    a.but{
     display: table;
     width: 100px;
     height: 100px;
     top: 45%;
     left: 1%;
   }

   #ch{
     display: table-cell;
     vertical-align: middle;
   }
   .container {
    text-align: left;
    overflow: hidden;
    width: 80%;
    margin: 0 auto;
    display: table;
    padding: 0 0 8em 0;
  }

  .container td, .container th {
    padding-bottom: 2%;
    padding-top: 2%;
    padding-left:2%;  
  }

  /* Background-color of the odd rows */
  .container tr:nth-child(odd) {
    background-color: #323C50;
  }

  /* Background-color of the even rows */
  .container tr:nth-child(even) {
    background-color: #2C3446;
  }

  .container th {
    background-color: #1F2739;
  }

  .container td:first-child { color: #A7A1AE; }

  .container tr:hover {
   background-color: #464A52;
   -webkit-box-shadow: 0 6px 6px -6px #0E1119;
   -moz-box-shadow: 0 6px 6px -6px #0E1119;
   box-shadow: 0 6px 6px -6px #0E1119;
 }

 .container td:hover {
  background-color: #FFF842;
  color: #403E10;
  font-weight: bold;
  
  box-shadow: #7F7C21 -1px 1px, #7F7C21 -2px 2px, #7F7C21 -3px 3px, #7F7C21 -4px 4px, #7F7C21 -5px 5px, #7F7C21 -6px 6px;
  transform: translate3d(6px, -6px, 0);
  
  transition-delay: 0s;
  transition-duration: 0.4s;
  transition-property: all;
  transition-timing-function: line;
}

@media (max-width: 800px) {
  .container td:nth-child(4),
  .container th:nth-child(4) { display: none; }
}

</style>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script type="text/javascript" src="js/converting.js"></script>
</head>

<body>
  <a href="/students" class="but fa" style="position: fixed; font-size: 45px; text-align: center;  z-index: 50;" id="back"><div id="ch">&#xf060;</div></a><br>
  <h1 style="display: table; margin: auto; padding: 20px"><%= title %></h1>
  <table cellpadding="0" cellspacing="0" border="0" class="container">
    <div class="tbl-header">
      <thead>
        <tr style="color:white;">
          <th>Name</th><th>Roll No.</th><th>Dept.</th><th>Email</th><th>Contact No.</th></tr></thead></div>
          <tbody>
            <% students.forEach(function(row) { %>
            <tr class='<%= row.dept %> <%= row.nameai %> <%= row.name %> <%= row.roll %> hide'>
              <td><%= row.name %></td><td><%= row.roll %></td>
              <td><%= row.dept %></td><td><%= row.email %></td>
              <td><%= row.tele %></td></tr>
              <% }); %>
            </tbody>
          </table>
          <img src="../images/iitp.png"/ id="logo">
          
          <script type="text/javascript">
            function start() {
              var flag=0;
              if(gloablLangTranslate=="hi")
              {
                flag=1;
              }
              changelang("en-US");
              console.log("before start");
              startConverting(null,function(msg) {
                msg = msg.toUpperCase();
                if(msg.indexOf("BACK")>=0 || msg.indexOf("PREVIOUS PAGE")>=0)
                  $("#ch").click();
                if(msg.indexOf("DONE")>=0 || msg.indexOf("FINISH")>=0 || msg.indexOf("TERMINATE")>=0 || msg.indexOf("END")>=0)
                  window.location.href = '/';
                $("#back").click();
                if(msg.indexOf("CS")>=0 || msg.indexOf("COMPUTER")>=0) {
                  $(".hide").hide();
                  $(".CSE").show();
                }
                else if(msg.indexOf("ME")>=0 || msg.indexOf("MECHANICAL")>=0) {
                  $(".hide").hide();
                  $(".ME").show();
                }
                else if(msg.indexOf("CB")>=0 || msg.indexOf("CHEMICAL")>=0) {
                  $(".hide").hide();
                  $(".CB").show();
                  $(".CH").show();
                }
                else if(msg.indexOf("CE")>=0 || msg.indexOf("CIVIL")>=0) {
                  $(".hide").hide();
                  $(".CE").show();
                } 
                else if(msg.indexOf("EE")>=0 || msg.indexOf("ELECTRICAL")>=0) {
                  $(".hide").hide();
                  $(".EE").show();
                }
                else if(msg.indexOf("ALL")>=0 || msg.indexOf("SAB")>=0) {
                  $(".hide").show();

                }
                {
                  msg = msg.split(" ");
                  var flag=0;
                  var x=0;
                  var arrayLength = msg.length;
                  if(arrayLength>1)
                  {
                    for (var i = 1; i < arrayLength; i++) {
                      msg[i] = msg[i-1]+msg[i];
                    }    
                  }
                  else
                  {
                    var hasClass = document.getElementsByClassName(msg[0]);
                    if(hasClass.length>0)
                    {
                      $(".hide").hide();
                      $("."+msg[0]).show();
                    }
                  }
                  console.log(msg);
                  var arrayLength = msg.length;
                  console.log(arrayLength)

                  for (var i = 1; i < arrayLength; i++) {
                    var hasClass = document.getElementsByClassName(msg[i]);
                    if(hasClass.length>0)
                    {
                      if(flag==0){
                        $(".hide").hide();
                        $("."+msg[i]).show();
                        flag=1;
                      }
                      else
                      {
                        $("."+msg[i]).show();
                      }

                    }   
                  }

                }
                setTimeout(  function( ){start();},5000 );
                console.log(msg);
              });
              if(flag==1)
              {
                changelang("hi");
              }
            }
            speak("Which department do you want to see?");
            setTimeout(  function( ){start();},3000 );
          </script>

        </body>
        </html>